# Questa cartella contiene le credenziali del database e i file interni: nessun accesso dal web.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
